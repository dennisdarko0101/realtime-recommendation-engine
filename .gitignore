__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
.env
# Anchored to the repo root so it ignores the generated ./data/ directory only,
# not the src/data/ source package (which an unanchored "data/" would swallow).
/data/
.mypy_cache/
.ruff_cache/
.pytest_cache/
htmlcov/
coverage.xml
.coverage
*.log
